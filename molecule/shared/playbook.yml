- name: converge
  hosts: localhost
  roles:
    - role: openstack_provision
      openstack_provision_servers: "{{ molecule_yml.platforms }}"

- name: run tests
  hosts: localhost
  tasks:
    - name: debug output
      debug: var=item
      with_items: "{{ groups['test_group'] }}"
