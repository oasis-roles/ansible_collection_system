users_and_groups_become: true
users_and_groups_become_user: root

# A list of new groups to create on the host machines
users_and_groups_add_groups: []

# A list of groups to remove/delete from the host machines
users_and_groups_remove_groups: []

# A list of users to add to specified groups. List items are formatted like so:
# {name: "John_Doe", groups: ["test_group1", "test_group2"]}
users_and_groups_add_users: []

# A list of users to remove from specified groups. List items are formatted
# like so:
# {name: "John_Doe", groups: ["test_group1", "test_group2"]}
users_and_groups_remove_users: []
