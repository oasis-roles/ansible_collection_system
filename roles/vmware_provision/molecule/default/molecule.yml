driver:
  name: delegated
platforms:
  - name: test-vmware_provision
provisioner:
  playbooks:
    converge: ./playbook.yml
    create: ../shared/create.yml  # Does not create/destroy
    destroy: ../shared/destroy.yml
