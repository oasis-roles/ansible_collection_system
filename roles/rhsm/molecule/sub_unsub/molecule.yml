driver:
  name: openstack
platforms:
  - name: rhsm-sub-unsub
    groups:
      - rhsm
provisioner:
  inventory:
    links:
      group_vars: ../shared/group_vars
  playbooks:
    converge: playbook.yml
